import { Container } from "@containers";
import "./style.scss"

const index = () => {
    return (
        <div>
            <Container>
                <h1 className="font-semibold text-[30px] p-4">Contact page</h1>
            </Container>
        </div>
    );
};

export default index;